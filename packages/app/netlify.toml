[build]
  base = "packages/app"
  command = "npm install && npm run build:app"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"